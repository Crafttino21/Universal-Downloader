# The CLI release workflow builds into repo-root dist/ and build/. Those are
# ephemeral on the runner, but keep them out of the way if the workflow's build
# step is ever reproduced locally.
/dist/
/build/

# Local deployment helper — personal tooling, deliberately not shared. It also
# has to stay ignored for its own "working tree is clean" check to pass.
/deployment.bat
